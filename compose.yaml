services:
  api:
    build: .
    image: parakeet-diarise:api
    gpus: all
    env_file:
      - .env
    volumes:
      - ./Output/.cache:/app/.cache
    working_dir: /app
    ports:
      - "${API_PORT:-8000}:${API_PORT:-8000}"
